# Pinned runtime. Every bench artifact records these versions.
# Installed inside the Docker image. Spark does not run on the host's Python 3.14 / Java 25.

# Stage 1, distributed batch
pyspark==3.5.3

# Stage 2, lakehouse / warehouse
duckdb==1.1.3
dbt-core==1.8.9
dbt-duckdb==1.8.4
pyarrow==17.0.0
pandas==2.2.3

# Stage 3, streaming
kafka-python==2.0.2
# kafka-python does NOT vendor its compression codecs: asking for compression_type='lz4'
# without this raises "Libraries for lz4 compression codec not found" at producer
# construction. It failed silently behind a redirected log the first time.
lz4==4.3.3

# Stage 4, graph
neo4j==5.27.0

# Stage 4b, vector search. Exact nearest neighbours over 1.6M vectors would be 2.5e12 distance
# computations, so the index is not a nicety.
faiss-cpu==1.9.0
numpy==1.26.4

# Tooling
pytest==8.3.4
ruff==0.8.4
