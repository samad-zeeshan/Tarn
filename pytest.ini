[pytest]
testpaths = tests
python_files = test_*.py
markers =
    integration: needs a live service (Neo4j / Redpanda) from docker-compose
# Spark is chatty and the JVM's stderr is not a test result.
filterwarnings =
    ignore::DeprecationWarning
addopts = -ra --strict-markers
